#pragma once

/*Download pugixml library from https://pugixml.org/ 
Extract files and add pugiconfig.h, pugixml.h and pugixml.cpp to project.
Create xml file inside project directory called employees.xml and fill it with 
data of your choosing, follow your hierarchy and get the data from nodes and 
create object of class Employee, add object to vector and print info about 
all objects in the vector.

Bonus create function for xml parsing or remove it from main and initialize it above.
Bonus create another node inside the Employee and create another class representing
this new node. Print information about all nodes.*/